# EPISODE3
- Learned about JSX 
- How babel is useful in conversion of JSX into  React Element
- JSX => (BABEL transpilation)=> React.CreateElement()=> ReactElement-JS Object=> HTML ELEMENT(Render)
- ReactDOM.render() : It takes two arguments, first one is the react element and second argument is where to render that      element i.e., a container
- React Components and React Functional Components
- Rendering Functional Component(<Component/>)
- Injection of React element inside Functional Componenets and all the combinations;
- <Comp/> == <Comp><Comp/> == {Comp()} 